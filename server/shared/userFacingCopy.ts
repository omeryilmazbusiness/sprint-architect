/**
 * User-visible notification and status labels (App Store neutral wording).
 * Internal types/API fields unchanged.
 */

export const PLAN_STEP_LABELS: Record<string, string> = {
  PRE_ARRIVAL: "Pre-Arrival Preparation",
  ARRIVAL_TRANSFER: "Arrival & Transfer",
  HOTEL_CHECKIN: "Hotel Check-In",
  TREATMENT: "Service",
  FOLLOWUP: "Follow-Up",
  DEPARTURE: "Departure",
};
