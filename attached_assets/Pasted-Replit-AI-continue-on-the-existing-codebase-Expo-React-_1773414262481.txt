Replit AI — continue on the existing codebase (Expo React Native + Express/TypeScript backend + PostgreSQL + Drizzle ORM). DO NOT re-scaffold.

TASK:
Implement and SHIP the Admin → Users selection workflow improvements (not just plan them). Add the features exactly as requested:
- “Select” button in Users list
- multi-select rows (checkboxes)
- “Delete selected” action (safe deactivation)
- “Cancel / Vazgeç” action while selecting to exit selection mode and clear selections
Make it premium UX, SOLID, and production-ready.

NON-NEGOTIABLE:
- White theme consistent with Admin.
- RBAC: ADMIN only.
- Deletion must be safe: SOFT delete/deactivate only (status=INACTIVE), not hard delete.
- Must NOT expose passwords.
- Must NOT allow deactivating:
  - the currently logged in admin
  - a clinic’s primary manager without reassignment (block and explain)
- Must include confirmation modal before deletion.
- Must show results: how many deactivated vs blocked.
- Must include loading states and error handling.

========================================================
A) IMPLEMENTATION — FRONTEND (Expo RN)
========================================================
Target screen:
- Admin Users list screen (where users are listed)

1) Header actions:
- Normal mode: show “Select”
- Selection mode: show “Cancel / Vazgeç”

2) Selection mode behavior:
- When “Select” tapped -> checkboxes appear in each row
- Tapping a row toggles its checkbox
- Show a sticky action bar (bottom or top) with:
  - “Selected: N”
  - “Delete selected” (disabled when N=0)
  - optional “Select all”
- Long press on a row should enter selection mode + select that row (pro UX)

3) Delete flow:
- Tap “Delete selected” -> confirmation modal:
  Title: “Deactivate users?”
  Body: “Selected users will be deactivated and lose access immediately.”
  Buttons: Cancel / Deactivate
- On confirm:
  - call backend bulk-deactivate
  - show success toast
  - show warning toast if some are blocked
  - refetch list
  - exit selection mode and clear selection

4) UI design (premium):
- List rows are modern cards:
  - big display name
  - under it: chips row (Clinic / Role / Status)
  - right: checkbox in selection mode, chevron otherwise
- Smooth transitions into selection mode
- Skeleton loading + empty state

5) Code quality:
- Create reusable hook:
  - hooks/useSelectionMode.ts
- Create reusable components:
  - components/users/UserCardRow.tsx
  - components/users/SelectionToolbar.tsx
  - components/modals/ConfirmModal.tsx
- Avoid giant screens; keep logic modular.

========================================================
B) IMPLEMENTATION — BACKEND (Express + Drizzle) SOLID
========================================================
If bulk-deactivate endpoint already exists, verify and fix rules; otherwise implement it.

1) Endpoint:
POST /v1/admin/users/bulk-deactivate
ADMIN only
Body (zod):
{
  targets: [{ id: string, entityType: "ADMIN" | "MANAGER" | "PATIENT" }]
}

2) Rules (use-case):
- Block if target is the current authenticated admin (BLOCKED_SELF)
- Block if target MANAGER is clinic.primaryManagerUserId (PRIMARY_MANAGER_DELETE_BLOCKED)
- For allowed targets:
  - set user.status=INACTIVE OR patient.status=INACTIVE
  - set statusReason="ADMIN_DEACTIVATED"
- Write audit log:
  - action: ADMIN_BULK_DEACTIVATE
  - metadata: counts + ids + blocked reasons
Return:
{
  deactivated: number,
  blocked: [{ id, entityType, reason }]
}

3) SOLID module structure (only for touched parts):
server/modules/adminUsers/
  routes/controller/usecases/repos/schemas
No Drizzle in controller; only repos.

========================================================
C) VERIFICATION (MANDATORY)
========================================================
1) Add/extend backend tests:
- cannot deactivate self
- blocks primary manager
- deactivates mixed targets
2) Manual QA steps:
- Select -> choose 3 users -> Delete selected -> confirm -> users become inactive
- Cancel clears selection
- Try delete self -> blocked message shown
- Try delete primary manager -> blocked list and message shown

========================================================
OUTPUT REQUIREMENTS
========================================================
After finishing, print:
1) Files changed/added (frontend + backend)
2) Endpoint + sample curl for bulk-deactivate
3) Screens updated and how selection mode works
4) Manual QA checklist
5) Tests added + how to run

IMPLEMENTATION ORDER:
1) Backend bulk-deactivate verification/fix + tests
2) Frontend selection mode + cancel + toolbar + confirm modal
3) Premium row UI
4) Final polish + run test suite

Proceed now and implement the features. Do not just describe.
END.