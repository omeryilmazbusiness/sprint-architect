Replit AI — continue on the existing codebase (Expo React Native + Express/TypeScript backend + PostgreSQL + Drizzle ORM). DO NOT re-scaffold.

PROBLEM:
When there is NO data (fresh launch, empty DB), the Admin Dashboard banner shows “failed data” / error state and looks broken.
We need a premium, consistent design where:
- Banner and overview always render nicely
- If data is empty, show 0 values and an elegant empty-state message (not “failed data”)
- Only show a true error when the API call actually fails (network/server error).

REQUEST:
1) Fix the dashboard data fetching + empty-state logic so empty data is not treated as error.
2) Redesign the banner carousel (top 1/3) to match premium white theme:
   - soft rounded corners like overview cards
   - richer layout (headline + subtitle + mini KPI chips + CTA)
   - consistent spacing, no harsh edges
3) Ensure a “0 default display” for all KPI metrics and lists.
4) Make it SOLID-friendly:
   - backend returns a stable dashboard DTO with defaults
   - frontend uses a dedicated hook and presentational components
5) Prod-ready:
   - robust error handling
   - skeleton loading
   - correct empty states

NON-NEGOTIABLE:
- White theme premium (off-white background + white cards + subtle shadow + rounded corners).
- No “failed data” shown for empty datasets.
- Use stable DTO defaults from backend.
- Use modular components, no giant dashboard file.
- Loading/empty/error states must be clearly separated.

========================================================
A) ROOT CAUSE CHECK (MANDATORY)
========================================================
1) Identify why banner shows “failed data”:
- Is backend returning 404/500 when DB has zero records?
- Is frontend treating empty arrays as failure?
- Is React Query throwing because parsing expects non-null fields?

2) Fix either:
- Backend: ensure /v1/admin/dashboard ALWAYS returns 200 with default values
- Frontend: ensure empty response is not treated as error

========================================================
B) BACKEND — STABLE DASHBOARD DTO WITH DEFAULTS (SOLID)
========================================================
If /v1/admin/dashboard exists:
- Ensure it returns a stable DTO even if DB is empty:
{
  currentPeriod: "YYYY-MM",
  clinics: { total: 0, active: 0, suspended: 0 },
  invoices: { pending: 0, unpaid: 0, paid: 0, totalBilledThisMonth: 0 },
  recentInvoices: [],
  activity: []
}
Return 200 always. Never throw just because tables are empty.

If endpoint is missing or not SOLID:
- Implement/repair using module:
server/modules/adminDashboard/
  controller -> usecase -> readRepo (drizzle)
- Repo should use COALESCE and safe defaults.

========================================================
C) FRONTEND — PREMIUM BANNER REDESIGN + EMPTY STATES
========================================================
1) Create modular components:
- components/adminDashboard/BannerCarousel.tsx
- components/adminDashboard/BannerSlide.tsx
- components/adminDashboard/OverviewKpis.tsx
- components/adminDashboard/RecentInvoices.tsx
- components/adminDashboard/ActivityFeed.tsx
- hooks/useAdminDashboard.ts (React Query)

2) Data handling rules:
- If query isLoading: show skeleton banner + skeleton KPI cards
- If query isError (network/500): show a premium error card:
  “We’re having trouble loading data. Retry.”
- If query isSuccess but values are zeros/empty lists:
  - show banner normally with 0 metrics
  - show small empty hints:
    - Recent invoices: “No invoices yet”
    - Activity: “No activity yet”

IMPORTANT:
- Do NOT show “failed data” on success responses with empty arrays.
- Provide default fallback in UI too:
  const pending = data?.invoices?.pending ?? 0

3) Premium banner style:
- Each slide is a rounded card (radius 20+), matching overview cards.
- Background: subtle gradient or light pattern (no heavy colors).
- Layout inside slide:
  - Headline (bold)
  - Subtitle (1 line)
  - 2 mini KPI chips (e.g., Pending: 0, Unpaid: 0)
  - CTA button (e.g., “View Invoices”)
- Indicator dots centered below.
- Ensure spacing and alignment are premium and consistent.

4) Empty DB “Launch state” microcopy (professional):
- Add a small non-intrusive message in the banner or below KPIs when everything is 0:
  “System is ready. Create your first clinic to get started.”
with CTA -> Create Clinic.

========================================================
D) FILTERED NAVIGATION STILL WORKS
========================================================
- Banner CTAs and KPI cards must route with filters when counts > 0.
- When counts are 0, CTA goes to the general list screen (no filters) OR to create flow:
  - Billing slide CTA: if unpaid>0 -> invoices?status=UNPAID else invoices
  - Clinics slide CTA: if suspended>0 -> clinics?status=SUSPENDED else clinics
  - Launch state CTA: create clinic

========================================================
E) TESTS + VERIFICATION
========================================================
1) Backend test:
- Empty DB -> GET /v1/admin/dashboard returns 200 with zeros and empty arrays
2) Frontend manual QA checklist:
- Fresh DB (no data) -> banner shows zeros, no error text
- Click CTAs -> navigate correctly
- Network failure simulated -> shows error card with Retry (not zero state)

========================================================
OUTPUT REQUIREMENTS
========================================================
After implementation print:
1) Root cause found (why “failed data” appeared)
2) Files changed/added
3) Example /v1/admin/dashboard response when DB empty
4) Manual QA checklist for empty DB + normal data + error scenario

IMPLEMENTATION ORDER:
1) Fix backend dashboard DTO defaults (always 200)
2) Refactor frontend to modular components + safe fallbacks
3) Redesign banner visuals (rounded, rich layout)
4) Add “launch state” microcopy + CTA
5) Add tests + final verification

Proceed now. Keep it premium and robust.
END.