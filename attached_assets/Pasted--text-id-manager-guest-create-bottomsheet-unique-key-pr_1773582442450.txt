```text id="manager_guest_create_bottomsheet_unique_key_prod_prompt"
Replit AI — continue on the existing codebase (Expo React Native + Expo Router + Express/TypeScript backend + PostgreSQL + Drizzle ORM). DO NOT re-scaffold.

REQUEST:
Build a premium “Create Guest” flow for Manager:
- Must open as a bottom-sheet popup (slide up) from the Users screen (not a standalone page).
- Collect the most necessary, professional guest/patient fields only.
- Include a 2nd contact (companion/refakatçi).
- Include requested service.
- Include arrival & departure dates (calendar picker).
- Include nationality/personal details.
- Phone input must be professional: country flag picker + calling code + formatted input (E.164 stored).
- For every new patient, system MUST generate a unique patientKey on backend and save it in DB (guests/patients table). Then show the key to the manager with copy.

NON-NEGOTIABLE:
- SOLID backend (controller -> usecase -> repo; DB only in repo).
- Strong validation both client and server.
- Clinic scoping: manager can only create guests in their own clinic.
- Empty/error states are premium (no “failed data”).
- patientKey generation must be collision-safe (unique constraint + retry).
- On success: show patientKey once + copy button + “Open Guest” CTA.

========================================================
PHASE 1 — AUDIT CURRENT SYSTEM (MANDATORY)
========================================================
1) Confirm which table stores guests:
- patients table or a dedicated guests table
2) Confirm if patientKey already exists and is unique in DB.
3) Identify current create endpoint and payload.
4) Report:
- file paths for current create UI
- backend route for create
- gaps to implement

Do not implement until audit is complete.

========================================================
PHASE 2 — REQUIRED FIELDS (PROFESSIONAL MINIMUM)
========================================================
Create Guest form fields:

A) Personal
- fullName (required)
- nationality (required) + nationalityCode (required)
- dateOfBirth (optional)
- passportNo (optional)
- preferredLanguage (optional)

B) Contact (Primary)
- phoneE164 (required) — flag picker + format
- email (optional)

C) Companion / 2nd Contact (optional)
- companionFullName (optional)
- companionPhoneE164 (optional) — same phone component
- companionRelation (optional)

D) Travel & Service
- arrivalDate (required)
- departureDate (required; must be >= arrivalDate)
- arrivalAirport (optional)
- flightNumber (optional)
- requestedService (required) — dropdown
- notes/about (optional multiline)

System-managed (not input):
- status default: PENDING
- patientKey generated server-side
- clinicId derived from manager session

Validation rules:
- required fields enforced
- phone must be valid E.164
- nationality required
- departureDate >= arrivalDate
- requestedService required

========================================================
PHASE 3 — BACKEND (SOLID) CREATE GUEST + UNIQUE patientKey
========================================================
Endpoint:
POST /v1/manager/patients
(Or /v1/manager/guests if your API uses “guests”; keep naming consistent.)

Zod schema for body must include the fields above.

Usecase rules:
1) Derive clinicId from authenticated manager (NOT from request).
2) Set status = PENDING on create.
3) Generate patientKey:
- Format: "PATIENT-" + 10 chars (A-Z0-9) OR "GUEST-" + YYYY + "-" + 6 chars
- Enforce DB unique constraint on patientKey.
- If collision occurs, retry max 5 times then throw APP error code KEY-001.

DB:
- Add/ensure unique index/constraint: patients.patientKey UNIQUE

Return DTO:
{
  id,
  fullName,
  clinicId,
  status,
  patientKey,
  arrivalDate,
  departureDate,
  requestedService,
  phoneE164,
  nationality,
  nationalityCode
}

SOLID module structure:
server/modules/managerPatients/
  managerPatients.routes.ts
  managerPatients.controller.ts
  schemas/managerPatients.schemas.ts
  usecases/CreatePatient.ts
  repos/ManagerPatientsRepo.ts
  repos/ManagerPatientsRepo.drizzle.ts
server/shared/errors (AppError + codes)
server/shared/tx (optional)

Tests:
- create generates patientKey
- patientKey unique constraint works
- clinic scoping works

========================================================
PHASE 4 — FRONTEND (EXPO) CREATE GUEST BOTTOM SHEET (PREMIUM)
========================================================
1) Entry point:
- Manager Users screen has “New Guest” button/FAB.
- Pressing it opens CreateGuestSheet bottom sheet.

2) Bottom sheet component:
components/managerGuests/CreateGuestSheet.tsx
- Sticky header: “New Guest” + close X
- Sections with clean spacing:
  - Personal
  - Contact
  - Companion (collapsible)
  - Travel & Service
  - Notes
- Submit button pinned at bottom: “Create Guest”
- Inline validation messages
- Loading state while submitting
- Keyboard safe (fields never hidden behind keyboard)

3) Phone input (flag + formatting):
Use existing libraries in project if present; otherwise add minimal:
- react-native-country-picker-modal
- libphonenumber-js
Behavior:
- Select country flag (searchable list)
- Auto set calling code
- Format number as user types
- Store E.164 in state and submit only E.164

4) Date pickers:
- Use modern date picker (arrival/departure).
- Enforce departure >= arrival.

5) Requested service dropdown:
- Bottom-sheet picker with options:
  - Dental, Eye, Rhinoplasty, Hair, Plastic, etc.
(Use same values backend accepts.)

6) On success:
- Close form sheet
- Show a success modal/sheet:
  “Guest created”
  - Patient Key (full) + Copy button
  - “Open Guest” button (navigate to guest detail)
- Invalidate guests list query so new guest appears immediately.

React Query:
- useMutation(createGuest)
- onSuccess invalidate ["manager","guests"] (or your query key)
- show toast

========================================================
PHASE 5 — OUTPUT REQUIREMENTS
========================================================
After implementing print:
1) Files changed/added
2) Endpoint + sample curl to create guest
3) patientKey format and uniqueness strategy
4) Manual QA checklist:
- bottom sheet opens and closes
- phone flag input works and stores E.164
- date validation works
- companion optional works
- guest created and patientKey shown + copy works
- guest appears in list
5) SOLID boundaries summary

IMPLEMENTATION ORDER:
1) Audit current table/endpoint
2) Backend: patientKey unique constraint + create usecase
3) Frontend: CreateGuestSheet + validations + phone/date inputs
4) Success key reveal + list refresh
5) Tests + verification

Proceed now. Implement and verify end-to-end.
END.
```
